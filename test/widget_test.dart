import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder – real tests live in test/unit/
    expect(true, isTrue);
  });
}
